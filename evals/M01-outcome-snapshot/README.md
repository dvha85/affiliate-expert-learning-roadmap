# M01 outcome-snapshot evaluator pack

`valid-outcome-snapshot.md` is a **structural synthetic evaluator fixture**.
It intentionally uses the required shape while documenting that it does not
establish a learner's E3 account/export/outcome evidence.

Run:

```bash
python scripts/validate_m01_outcome_snapshot_pack.py
```

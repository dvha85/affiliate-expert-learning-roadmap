# M05 starter — reviewed improvement

Use this pack to model one frozen experiment and a human-reviewed change
proposal. It intentionally cannot publish, change policy, modify a prompt or
release a workflow.

```bash
python scripts/validate_m05_reviewed_improvement_pack.py
```

Copy the templates to `workspace/` before entering a real trace. Synthetic
evaluator cases prove safety/linkage only, not E4.

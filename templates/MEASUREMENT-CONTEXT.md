# Measurement Context — copy to local/private first

- measurement_context_id:
- origin: real
- action_record_id:
- tracking_id:
- measurement_source/reference:
- window_start:
- window_end:
- metric_scope:
- attribution limitation:
- next_read_at:

This context is declared before reading outcome. Changing scope/window requires
a new context ID rather than editing the historical context.
